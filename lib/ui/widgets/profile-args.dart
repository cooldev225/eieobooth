import 'package:flutter/cupertino.dart';

class Arguments {
  dynamic resident;
  ImageProvider<dynamic> avatar;
  // dynamic addintional;
  // Arguments(this.resident);
  Arguments(this.resident, this.avatar);
}
class Arguments3 {
  dynamic resident;
  ImageProvider<dynamic> avatar;
  dynamic addintional;
  // Arguments(this.resident);
  Arguments3(this.resident, this.avatar,this.addintional);
}
class Token {
  String token;
}
